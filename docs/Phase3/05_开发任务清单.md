# Phase 3 开发任务清单

> 优先级：P0 = 阻塞后续 / P1 = 核心功能 / P2 = 体验增强

---

## 当前进度（2026-04-27）

### 已完成

- **Phase 3+ 规划（已确认）**：[`06_对齐WeKnora界面及智能推理.md`](06_对齐WeKnora界面及智能推理.md) — 会话列表（WeKnora 式）、`agent_mode` 真分支/契约、**快速问答延迟（Phase P）** 等；2026-04-27 文档整体已确认，实施按文内 Phase 顺序与 §7 分项 gate 推进。
- **人工验证（2026-04-27）**：`/`、`/admin`、`/login` 壳；`ULTRARAG_ADMIN_TOKEN` 未配置 / 已配置下的 `/admin` 与 `/api/kb/` 行为；Chrome 375/390 设备栏对话与管理窄屏；首页主路径（含三 Demo 库、流式、Shift+Enter、500 字限制）；有/无插图时 `sources` 与「引用来源」区展示策略。详见下文「人工验证（2026-04-27 已通过）」。
- **Sprint 4（补充）**：管理端上传 **MIME + 扩展名** 校验（`utils/uploadGuards.js`）；全站 **favicon.svg** 与 **title** 区分对话/管理/登录。
- **Sprint 4（收尾）**：`components/toast.js` 全局右上角 Toast；管理页 `showFlash` 已改为 Toast；对话流 `meta.degraded` 时仅提示一次 `info` Toast（5s）。**P1**：管理列表/详情 **骨架屏**、**空状态**、失败 **Toast**、对话 **「重试」**；`style.css` 骨架 shimmer、思考中三点、重试间距。**P2**：超大 base64 **延迟赋 src**（`brokenImagePlaceholder.js` + Lightbox `getImageEffectiveSrc`）。**收尾**：`components/kbSelector.js`、`components/agentSelector.js` 从 `main.js` 抽出；对话/管理页 **移除未使用的 Vue 脚本**（仅保留 `marked` + `DOMPurify`，避免多余全局与体积）；流式 **尾部光标**（`message-content--streaming`）；推理区 **首次展开轻动画**。
- **Sprint 1 补充**：`ultrarag_agent_mode` **localStorage** 恢复与变更写入；流式首包前 **思考中 + 三点**（`.thinking-indicator` / `.typing-dot`），首 chunk 后清除。**停止生成**：`AbortController` + `chatApi.sendChatMessage({ signal })`；发送按钮流式期间为 **「停止」**（`send-button--stop`）并可点击中断。**`components/chatMessage.js`** 承载气泡 DOM 与 Markdown/思考区。**管理页 `innerHTML`** 经 `utils/sanitizeHtml.js`（`DOMPurify.sanitize`）+ `admin.html` 预加载 DOMPurify。
- **Sprint 3（本轮）**：管理后台闭环：`#/kb/<id>` hash 路由、知识库卡片/详情、新建 Modal、`StatusBadge`、`ConfirmModal`、文档上传与删除、`kbApi` 全量封装、列表/详情 `document_count`、索引任务 `getJobProgress` 轮询；后端 `DELETE` 单文档 + 列表/详情 `document_count` 字段；`pytest` + `npm run test:cov` 已通过。
- Phase 3 前端基础目录与测试基建已建立：
  - 根目录新增 `package.json` / `package-lock.json` / `vitest.config.js`
  - 前端测试使用 `Vitest + happy-dom + @vitest/coverage-v8`
- `frontend/vendor/` 已本地化：
  - `vue.global.prod.js`：Vue `3.4.38`
  - `marked.min.js`：marked `9.1.6`
  - `DOMPurify.min.js`：DOMPurify `3.1.6`
  - `frontend/vendor/README.md` 已记录来源 URL 与 SHA256
- Flask 静态入口已完成：
  - `/`、`/chat` → `index.html`
  - `/admin`、`/admin/` → `admin.html`
  - `/login`、`/login/` → `login.html`
  - `/frontend/<path>` → `frontend/` 目录
  - 已追加基础安全响应头与 CSP
- 管理入口已增加可选 token 保护：
  - 未配置 `ULTRARAG_ADMIN_TOKEN` 时保持内网免登录
  - 配置后 `/admin` 未授权跳转 `/login`
  - 配置后 `/api/kb*`、`/api/roles*` 未授权返回 `401 JSON`
  - 管理 API 只接受 `X-Admin-Token`，不接受 Cookie token
- `services/chatApi.js` 已完成第一版：
  - 支持 `agent_mode`
  - 支持 `meta` / `thought` / `tool_start` / `tool_result` / `chunk` / `sources` / `done` / `error`
  - 支持 SSE 分片、CRLF、末尾未带空行的 buffer flush
  - 覆盖 HTTP 错误、无流式 body、fetch 异常等错误路径
- `services/kbApi.js` 与首页交互已完成第一版：
  - 调用 `GET /api/kb`
  - 兼容现有 `active` 知识库状态，并保留 `ready` / `indexed`
  - 前端只保留 UI 必要字段，不渲染 `data_path` / `index_path` / `embedding_path`
  - 下拉框渲染已有知识库，localStorage 记忆上次选择
  - 切换知识库清空消息；新建对话清空消息但保留当前知识库
  - 输入框支持 Enter 发送、Shift+Enter 换行、500 字限制、流式期间禁用输入/选库/新建
  - 发送时携带当前 `kbId` 与 `agentMode`，并流式渲染 AI 回答
- **Sprint 2**：**Lightbox**（含主回答区 Markdown 内嵌图）；后端 `answer` 由 **answer_blocks** 拼 Markdown；**有插图时 `sources` 为空**（不再展示下方「引用来源」重复区），条数见 `meta.retrieval_source_count`；`prompt` / `generation` 强化中文输出
- 已落地页面壳：
  - `index.html`：Nous 式 `Sidebar + Workspace + Composer` 骨架
  - `admin.html`：管理后台 `Header + Sidebar + Content` 骨架
  - `login.html`：内网模式无需登录提示 + Phase 4 隐藏表单
  - `style.css`：设计 Token、Reset、基础布局、安全区与移动端断点基础样式

### 测试记录

- `conda` 环境：`conda activate ultrarag`
- 已在 `ultrarag` 环境安装并验证 `uv 0.11.7`
- 后端/路由测试：
  - 命令：`conda run -n ultrarag python -m pytest tests/test_phase3_static_routes.py -v`
  - 结果：`15 passed`
- 前端测试与覆盖率：
  - 命令：`npm run test:cov`
  - 结果：`131 passed` 量级（含 `kbSelector` / `agentSelector`、流式光标、大 data URL 延迟加载等；以本地 `npm run test:cov` 为准）
  - 覆盖率：Statements `≈95%`，Branches `≈81%`，Functions `≈97%`，Lines `≈98%`（以本地 `npm run test:cov` 汇总为准）
- Lint：
  - `ReadLints` 检查相关改动文件：无错误
- 代码审查：
  - 已完成代码审查与安全复审，当前改动无阻塞问题

### 人工验证（2026-04-27 已通过）

- [x] 启动后端后浏览器打开 `/`、`/admin`、`/login` 均能正常显示页面壳
- [x] 未配置 `ULTRARAG_ADMIN_TOKEN` 时，内网免登录访问 `/admin` 符合预期
- [x] 配置 `ULTRARAG_ADMIN_TOKEN` 后：`/admin` 直开跳转 `/login`；带 `X-Admin-Token` 请求 `/api/kb/` 返回正常数据；不带 token 请求 `/api/kb/` 返回 `401`
- [x] Chrome DevTools 设备栏 375 / 390：对话页无横向整页溢出；汉堡可开关侧栏；Composer 底部安全区不遮挡；管理页窄屏骨架可读
- [x] 首页主路径：下拉可见 `AGV Demo 3`、`AGV Demo`、`Phase2 Demo`；选库后发消息 / Enter 出现用户气泡与 AI 流式回答；Shift+Enter 换行不发送；超 500 字不发且输入框红框
- [x] 有插图主回答：`sources` 为空且不再出现「📄 引用来源」折叠区；无插图时仍可下发完整 `sources` 与卡片

---

## Sprint 1：基础框架 + 对话页（约 3 天）

### 环境搭建

- [x] **P0** 下载 vendor 依赖到 `frontend/vendor/`
  - `vue.global.prod.js`（Vue 3.4.x）
  - `marked.min.js`（9.x）
  - `DOMPurify.min.js`（3.x）
  
- [x] **P0** 在 Flask `app.py` 注册静态路由
  - `GET /` → `index.html`
  - `GET /admin` → `admin.html`
  - `GET /login` → `login.html`
  - `GET /frontend/<path>` → `frontend/` 目录

- [x] **P0** 创建 `frontend/style.css`（设计 Token、Reset、基础布局）

### 对话页

- [x] **P0** `index.html` + `main.js`：骨架布局（**Nous 式**：左侧 `Sidebar` + 右侧 `Workspace`；Workspace 内 `MessageList` + 底栏 `Composer`；见 `03` §1）
- [x] **P0** 响应式基座：`viewport` meta；**768px 以下**侧栏改为**抽屉**（汉堡开关 + 点击遮罩关闭）；Composer `safe-area` 底内边距（见 `03` §0）
  - 进度：`viewport`、移动端断点、Composer `safe-area`、汉堡开关、`chat-sidebar-backdrop` 点击关闭 + `Escape` 关闭已完成；DevTools 375/390 已验收，发版前可在真机再抽查。

- [x] **P0** `components/KbSelector.js`（挂载在**对话页左侧边栏**）
  - 挂载时调 `GET /api/kb`（或文档约定带尾斜杠路径），过滤 `status=ready` 的知识库
  - 选择时触发 `onSelect(kbId)` 事件
  - 无知识库时显示空状态 + 管理后台链接
  - localStorage 记忆上次选择
  - 实现说明：`components/kbSelector.js` 提供 `populateKbSelect` / `KB_STORAGE_KEY`；`main.js` 负责 `GET /api/kb` 与 `change` 时写回 storage、重置会话。

- [x] **P0** `services/chatApi.js`：封装 SSE 流式请求
  - `sendChatMessage({ kbId, question, agentMode, onMeta, onThought, onToolStart, onToolResult, onChunk, onSources, onDone, onError })`（字段以 `04_API对接设计` 为准；未传 `agentMode` 时默认 `quick`）

- [x] **P1** **智能体选择（对话时切换）** — 对齐 WeKnora 底栏交互
  - `components/AgentSelector.js`（或并入 Composer）：下拉项 **快速问答**（`quick`）、**智能推理**（`agent`）；可选第三项「数据分析师」暂不实现则 **灰显 + Tooltip「即将推出」** 或隐藏
  - 选中值写入 `localStorage`（如 `ultrarag_agent_mode`），新开标签页保持默认
  - 发消息时把 `agent_mode` 随 `POST /api/chat` 提交；处理 SSE `meta`：`degraded === true` 时 Toast 一次 `message`（见 `04` 降级约定）
  - 预留回调：`onThought` / `onToolStart` / `onToolResult` 接到事件时再驱动步骤 UI（后端未发事件时无操作即可）
  - 实现说明：`components/agentSelector.js`：`mountAgentSelect`（追加禁用项「数据分析师（即将推出）」+ `title`）、`applyStoredAgentMode`；`main.js` 绑定 `change` 写 `ultrarag_agent_mode`。

- [x] **P2** **智能推理步骤区（占位）**：主区在 AI 气泡上方或气泡内可折叠区域「推理步骤」；无 `thought`/`tool_*` 事件时可隐藏；**耗时统计**等仍待后端全量事件后再做
  - 实现说明：`main.js` 在每条流式 AI 消息插入 `<details class="reasoning-steps">`；`onThought` / `onToolStart` / `onToolResult` 追加日志；无内容时在 `onDone` 移除；`onError` 整段移除；`style.css` 对首次展示的 `.reasoning-steps:not([hidden])` 增加轻量 `reveal` 动画。

- [x] **P0** `components/ChatMessage.js`：对话气泡
  - AI 气泡：左对齐，白色背景，`max-width: 72%`
  - 用户气泡：右对齐，蓝色背景
  - 流式输出时显示光标闪烁
  - 流结束后调 `marked.parse()` 渲染 Markdown（DOMPurify 过滤）
  - 实现说明：`components/chatMessage.js`：`createMessageElement` 在流式 AI 消息上设 `message-content--streaming`（首包前与思考中并存时由 CSS 隐藏尾部光标）；`clearStreamingCursor` 在 `onDone` / `onAbort` / `onError` 与切 Markdown 前调用；`renderMarkdownContent` / `clearThinkingIndicator` 等同前。

- [x] **P1** 输入框交互
  - Enter 发送，Shift+Enter 换行
  - 流式输出期间禁用输入 + 显示停止按钮
  - 字数限制 500，超限变红
  - 实现说明：流式期间已禁用输入、选库、新建与智能体下拉；**发送钮改为「停止」**并触发 `AbortController.abort()`；`chatApi` 支持 `signal` / `onAbort`。

- [x] **P1** 思考中动画（三点跳动，等待首个 chunk）
  - 实现说明：`main.js` 流式 AI 气泡 `thinking: true` 时注入 `.thinking-indicator` + `.typing-dots`；`onChunk` 首包调用 `clearThinkingIndicator`；样式见 `style.css`。

- [x] **P1** 新建对话按钮（位于**侧栏**；清空 messages，保留当前 `kb_id`，输入框焦点）

---

## Sprint 2：来源面板 + 图片展示（约 2 天）

- [x] **P0** 引用来源卡片（实现文件：`components/sourcePanel.js` 内 `createSourceCardElement`，后续可拆为独立 `SourceCard.js`）
  - 展示文档标题、段落摘要（3 行省略）
  - 图片缩略图行（约 `128×96px`，`overflow-x: auto`）
  - 左侧蓝色竖条样式

- [x] **P0** 来源面板折叠/展开
  - 默认折叠，显示"📄 引用来源（N 处）▾"
  - 点击展开显示所有 SourceCard

- [x] **P1** Lightbox 图片放大（`components/imageLightbox.js` + 消息列表委托点击 `.source-card__thumb`）
  - 点击缩略图打开全屏覆盖层
  - 显示大图，支持多图左右切换
  - ESC / 背景点击关闭，方向键切换

- [x] **P2** 图片加载失败占位图（避免破图显示）
  - 实现说明：`utils/brokenImagePlaceholder.js`；`sourcePanel.js` 缩略图与 `main.js` 流式结束后的 Markdown 正文图均 `bind` / `attachMarkdownImageFallbacks`；失败时替换为内联 SVG data URL。

---

## Sprint 3：管理后台（约 4 天）

### 框架

- [x] **P0** `admin.html` + `admin.js`：侧边栏 + 内容区布局
  - 侧边栏：知识库管理、系统状态导航项
  - Header：Logo + 返回对话页链接
- [x] **P1** 管理页 **768px 以下**：导航侧栏抽屉化；表格 `overflow-x:auto` 或小屏卡片布局（二选一须在 `03`/`评审` 定稿）
  - 已实现：顶栏 ☰、`admin-sidebar` 抽屉、`admin-sidebar-backdrop` 点击与 `Escape` 关闭；文档表外包 `admin-table-wrap`。

- [x] **P0** `services/kbApi.js`：封装所有知识库 REST 请求
  - 含：`listKnowledgeBases`（`purpose: 'chat'|'admin'`）、`createKnowledgeBase`、`getKnowledgeBase`、`deleteKnowledgeBase`、`listDocuments`、`deleteDocument`、`uploadKbDocuments`（XHR 进度）、`createIngestJob`、`listJobs`、`getJobProgress`、`getStoredAdminToken`。

### 知识库列表页

- [x] **P0** 知识库卡片列表
  - 调 `GET /api/kb`（`purpose: 'admin'`）获取数据
  - 每张卡片：名称、`kb_id`、文档数（后端 `document_count`）、状态徽章、`详情`（`#/kb/<id>`）/ `删除`（软删）
  - 说明：当前后端无单独「类型」字段，卡片未展示类型列。

- [x] **P0** `components/StatusBadge.js`：状态徽章（building/ready/error/pending）

- [x] **P0** 新建知识库 Modal
  - 表单：`kb_id`（必填）、名称（必填）、描述（可选）；调 `POST /api/kb`，成功后跳转 `#/kb/<id>` 详情
  - 说明：创建接口无「类型」字段，未做类型下拉（与 Phase2 API 一致）。

- [x] **P1** 删除知识库确认弹窗（`components/ConfirmModal.js`）
  - 调 `DELETE /api/kb/<id>`，成功后回列表

### 知识库详情页

- [x] **P0** 详情头部：名称、状态、文档数、块数（取最近成功 ingest 任务的 `result.chunk_count`，无则显示 —）、`重建索引` / `删除知识库` 按钮

- [x] **P0** 文档列表表格
  - 列：文件名、类型、状态、上传时间、操作（删除）；调 `GET /api/kb/<id>/documents` 渲染
  - 说明：`kb_documents` 无文件大小字段，未列「大小」列。

- [x] **P0** 文件上传（当前内联于 `admin.js` 详情区；可后续拆为 `components/FileUpload.js`）
  - 拖拽 + 点击、`.docx`/`.pdf`、单文件 ≤50MB、`XMLHttpRequest` 进度条、成功后刷新文档列表

- [x] **P1** 索引状态轮询（3 秒间隔）
  - 详情页存在运行中/待处理 ingest 任务或提交重建后，对 `.../jobs/<id>/progress` 轮询，终态后刷新详情并停止。

- [x] **P1** 删除文档确认弹窗 + 操作
  - 后端新增 `DELETE /api/kb/<kb_id>/documents?doc_id=...`（路径校验在 `data_path` 下删文件）。

---

## Sprint 4：体验打磨（约 2 天）

- [x] **P0** `components/Toast.js`：全局 Toast 通知
  - 右上角固定，自动消失（success 3s, error 5s, info 4s；`main.js` 中降级提示用 info 且 5s）
  - 调用方式：`Toast.show('message', 'success')` 或 `import { Toast } from '.../toast.js'`

- [x] **P1** 加载状态
  - 知识库列表：骨架屏（3 个灰色占位卡片）
  - 文档列表：表格行骨架屏
  - 实现说明：`admin.js` 中 `buildAdminListSkeleton` / `buildAdminDetailSkeleton`；`style.css` 中 `.skeleton-line`、`.admin-skeleton-list`、`.admin-table-skeleton` 等。

- [x] **P1** 空状态
  - 知识库列表为空：引导图 + "新建第一个知识库"按钮
  - 文档列表为空：引导图 + "上传第一份文档"提示
  - 实现说明：列表 `.admin-empty` + Emoji 图标占位 + 按钮跳转新建；详情表内 `.admin-empty-inline` 文案引导上传。

- [x] **P1** 网络错误处理
  - 对话失败：气泡显示错误信息 + [重试]按钮
  - API 请求失败：Toast 展示错误信息
  - 实现说明：`main.js` `onError` → `Toast.show` + `.message-retry`；知识库加载失败 `loadKnowledgeBases` → Toast；管理页列表/详情请求失败同步 Toast（`admin.js`）。

- [x] **P1** 登录页（`login.html`）
  - 当前显示"内网模式，无需登录"提示
  - 保留登录表单 HTML 结构（被 `display:none` 隐藏，Phase 4 启用）

- [x] **P1** 手机端联调自测（Chrome DevTools 设备栏）：375 / 390 宽度下跑通对话与管理主路径；修复横向溢出、不可点区域
  - 验收说明：2026-04-27 与上文「人工验证」设备栏项一并通过（对话 + 管理窄屏可读；主路径与插图 / 无插图 `sources` 行为已验）。

- [x] **P2** 页面 favicon 和 title 设置
  - 实现说明：`frontend/favicon.svg`；`index.html` / `admin.html` / `login.html` 增加 `<link rel="icon">`；`title` 统一为「AGV 知识库助手 · 对话/管理/登录」。

---

## 非功能性任务

- [x] **P0** 所有 `innerHTML` 插入内容通过 `DOMPurify.sanitize()` 过滤（防 XSS）
  - 实现说明：对话 Markdown 路径已在 `chatMessage.js` 使用 `DOMPurify`；管理页通过 `utils/sanitizeHtml.js` + `admin.html` 引入 vendor DOMPurify 包裹各 `innerHTML` 赋值。
- [x] **P1** 文件上传验证（MIME 类型校验，不仅依赖扩展名）
  - 实现说明：`utils/uploadGuards.js` 中 `isAllowedKbUploadFile`；`admin.js` 上传前校验；允许空 MIME / `octet-stream` 时仍须 `.pdf`/`.docx` 扩展名。
- [x] **P1** 控制台无 Vue 警告和 JS 错误（Chrome DevTools 检查）
  - 实现说明：对话页 `index.html`、管理页 `admin.html` 已移除未使用的 `vue.global.prod.js` 引用（当前对话与管理均为原生 DOM + 模块脚本）；验收以 DevTools Console 为准（2026-04-27 起与人工验收一并通过）。
- [x] **P2** 大图片 base64 超过 1MB 时显示"图片加载中"占位（避免页面卡顿）
  - 实现说明：`utils/brokenImagePlaceholder.js` 中 `estimateDataUrlDecodedBytes`、`deferOversizedDataUrlIfNeeded`、`applyImageSrcMaybeDeferred`、`getImageEffectiveSrc`；Markdown 经 `attachMarkdownImageFallbacks`、缩略图经 `sourcePanel.js`、Lightbox 主图经 `imageLightbox.js` 的 `show()`；阈值 `LARGE_DATA_URL_THRESHOLD_BYTES = 1_000_000`。

---

## 开发验收 Checklist

在提测前，逐项确认（**已全部确认**，与 **2026-04-27 人工验证** 及后续验收结论一致）。

### 对话页验收

- [x] 选择知识库后能正常发送消息
- [x] 智能体下拉可选「快速问答 / 智能推理」；请求带 `agent_mode`；若收到 `meta.degraded` 仅提示一次且不中断流式回答
- [x] 流式输出逐字显示，无乱码
- [x] Markdown 表格正确渲染
- [x] 来源卡片正确显示引用片段（含无插图完整 `sources` / 有插图不重复展示引用区）
- [x] 图片可点击放大，Lightbox 可关闭
- [x] 切换知识库后对话历史清空

### 管理后台验收

- [x] 能创建新知识库（表单验证正常）
- [x] 能上传 DOCX/PDF 文件（带进度条）
- [x] 索引构建中状态正确展示（旋转图标）
- [x] 索引完成后状态自动刷新
- [x] 能删除文档（有确认弹窗）
- [x] 能删除知识库（有确认弹窗）

### 稳定性验收

- [x] 断网时有友好错误提示，不白屏
- [x] 后端返回 500 时有 Toast 错误提示
- [x] 发送空消息时输入框有提示，不发请求
- [x] 上传超大文件（>50MB）时有提示，不发请求

### 响应式验收（手机）

- [x] 390px / 375px 宽对话：布局不横向溢出、抽屉侧栏可开关、Composer 安全区、发消息与流式回答
- [x] 390px / 375px 宽对话：展开来源、打开 Lightbox
- [x] 390px / 375px 宽管理：页面壳在窄屏下可读、无整页横向溢出
- [x] 390px / 375px 宽管理：详情内上传与索引进度全路径
