# AGV QA pipeline reference (Phase 1)
servers:
  retriever: servers/retriever
  prompt: servers/prompt
  generation: servers/generation

pipeline:
- retriever.retriever_search
- prompt.qa_rag_boxed
- generation.generate
